services:
  postgres:
    image: postgres:16
    container_name: skillbridge-postgres
    environment:
      POSTGRES_DB: skillbridge
      POSTGRES_USER: skillbridge
      POSTGRES_PASSWORD: skillbridge
    ports: ['5432:5432']
    volumes: [skillbridge_data:/var/lib/postgresql/data]
volumes:
  skillbridge_data:
