# Backend

The canonical FastAPI backend will be integrated here in Phase 1. Phase 0 keeps this
directory limited to environment scaffolding and does not add feature code.

Copy `.env.example` to `.env` and provide local values when the backend is introduced.
